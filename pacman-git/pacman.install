#!/bin/sh
# arg 1:  the new package version
# arg 2:  the old package version
post_upgrade() {
  # upgrade pacman db for 3.5.0 changes
  if [ "$(vercmp $2 3.5.0)" -ge 0 ]; then
		pacman-db-upgrade
	fi
}

